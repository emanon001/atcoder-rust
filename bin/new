#!/bin/bash -eu

cd "$(dirname $0)"
cd ..

cargo atcoder new "${1}" && mkdir "${1}/.vscode" && cp ./competitive/competitive.code-snippets "${1}/.vscode" && git add "${1}" && git commit -m "cargo atcoder new ${1}"
