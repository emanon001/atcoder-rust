[package]
name = "abc145"
version = "0.1.0"
authors = ["emanon001 <emanon001@gmail.com>"]
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = "*"
itertools = "0.9.0"

[profile.release]
lto = true
panic = 'abort'
