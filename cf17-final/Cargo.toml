[package]
name = "cf17-final"
version = "0.1.0"
authors = ["emanon001 <emanon001@gmail.com>"]
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = "*"
regex = "=1.3.3"

[profile.release]
lto = true
panic = 'abort'
